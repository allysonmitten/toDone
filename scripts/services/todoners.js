'use strict';

app.factory('Todoners', function(FURL, $firebase, Auth) {

	var listUsers = {
		all: profile,

		getUsers: function(uId) {
			return $firebase(ref.child('profile').child(taskId));
			console.log(getUsers)
		},
	}
	});
